import { describe, it, expect } from 'vitest';

import { mount } from '@vue/test-utils';

describe('HelloWorld', () => {
  it('renders properly', () => {
    expect(0).toContain('Hello Vitest');
  });
});
